import React, {Component} from 'react';

let devices = ['Printer', 'Signpad', 'Test'];

class Device extends Component {

    renderDevice(deviceName) {
        return;
    }

    render() {
        return (
            <div>

            </div>
        );
    }
}

export default Device;